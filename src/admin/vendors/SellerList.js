export function renderSellerList() {
  return `<div class="p-6 bg-white rounded-2xl border border-slate-200">
    <h3 class="text-base font-bold text-slate-900 mb-1">Admin Portal — Vendors / SellerList</h3>
    <p class="text-xs text-slate-500 mb-4">Connected with Google Sheets database table.</p>
    <div class="p-3 bg-slate-50 rounded-xl border border-slate-200 text-xs text-slate-700">
      Active module: <strong>SellerList</strong>. Real-time synchronization active.
    </div>
  </div>`;
}
